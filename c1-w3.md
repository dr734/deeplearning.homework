(previous content retained)

---

### 4.4 - 将一切合并到 nn_model()

在上面的函数（初始化、前向传播、计算成本、反向传播、更新）都实现之后，我们可以把它们合并到一个名为 `nn_model()` 的函数中，该函数将训练神经网络并返回学到的参数。

```python
# GRADED FUNCTION: nn_model


def nn_model(X, Y, n_h, num_iterations=10000, print_cost=False):
    """
    Arguments:
    X -- dataset of shape (2, number of examples)
    Y -- labels of shape (1, number of examples)
    n_h -- size of the hidden layer
    num_iterations -- Number of iterations in gradient descent loop
    print_cost -- if True, print the cost every 1000 iterations

    Returns:
    parameters -- parameters learnt by the model. They can then be used to predict.
    """

    np.random.seed(3)
    n_x = layer_sizes(X, Y)[0]
    n_y = layer_sizes(X, Y)[2]

    # Initialize parameters
    parameters = initialize_parameters(n_x, n_h, n_y)

    # Loop (gradient descent)
    for i in range(0, num_iterations):

        # Forward propagation
        A2, cache = forward_propagation(X, parameters)

        # Cost function
        cost = compute_cost(A2, Y, parameters)

        # Backpropagation
        grads = backward_propagation(parameters, cache, X, Y)

        # Update parameters
        parameters = update_parameters(parameters, grads, learning_rate=0.01)

        # Print the cost every 1000 iterations
        if print_cost and i % 1000 == 0:
            print("Cost after iteration %i: %f" % (i, cost))

    return parameters
```

### 4.5 - 预测（predict）

给定训练得到的参数，你可以用前向传播来对新的输入做出预测。

```python
# GRADED FUNCTION: predict


def predict(parameters, X):
    """
    Using the learned parameters, predicts a class for each example in X

    Arguments:
    parameters -- python dictionary containing your parameters
    X -- input data of size (n_x, m)

    Returns
    predictions -- vector of predictions of our model (0/1)
    """

    A2, cache = forward_propagation(X, parameters)
    predictions = (A2 > 0.5)

    return predictions
```

### 4.6 - 训练模型并可视化决策边界

接下来，训练具有隐藏层大小为 4 的神经网络，并绘制决策边界、打印准确率。

```python
# Train the model
parameters = nn_model(X, Y, n_h=4, num_iterations=10000, print_cost=True)

# Plot the decision boundary
plot_decision_boundary(lambda x: predict(parameters, x.T), X, Y)
plt.title("Decision Boundary for hidden layer size 4")

# Print accuracy
predictions = predict(parameters, X)
print(
    "Accuracy: %d "
    % float(
        (np.dot(Y, predictions.T) + np.dot(1 - Y, 1 - predictions.T))
        / float(Y.size)
        * 100
    )
    + "%"
)
```

你会看到神经网络比逻辑回归表现得更好，因为它能学习到非线性边界。

### 4.7 - 尝试不同的隐藏层大小

试验不同的隐藏层大小，观察决策边界与性能如何变化。

```python
# This may take a while depending on num_iterations
for n_h in [1, 2, 3, 4, 5, 20, 50]:
    parameters = nn_model(X, Y, n_h, num_iterations=5000)
    plt.title("Decision Boundary for hidden layer size " + str(n_h))
    plot_decision_boundary(lambda x: predict(parameters, x.T), X, Y)
    plt.show()
    print("Hidden layer size: {}".format(n_h))
    predictions = predict(parameters, X)
    print(
        "Accuracy: %d "
        % float(
            (np.dot(Y, predictions.T) + np.dot(1 - Y, 1 - predictions.T))
            / float(Y.size)
            * 100
        )
        + "%"
    )
```

你会发现：
- 隐藏层过小（如 1）时，模型欠拟合；
- 适中的隐藏层大小（例如 4 或 5）通常效果较好；
- 隐藏层过大（例如 50）可能会导致过拟合或不必要的复杂性。

## 5 - 结论与后续步骤

在本作业中，你实现了一个带单隐藏层的神经网络，用于解决一个二分类的平面数据问题。你学会了：
- 定义网络结构并初始化参数；
- 实现前向传播和计算交叉熵损失；
- 实现反向传播来计算梯度；
- 使用梯度下降更新参数并训练模型；
- 使用不同隐藏层规模实验模型的性能。

接下来你可以：
- 尝试不同的激活函数（如 ReLU），观察性能差异；
- 增加层数，构建更深的网络；
- 对模型加入正则化（L2、dropout），以减少过拟合；
- 在更多数据集上测试模型。

---

（注：以上翻译基于原始笔记本常见实现与结构整理，代码单元保持原样呈现以便复制粘贴执行。如果你希望我把代码严格与原仓库完全逐字对应，我可以再次从源仓库读取完整笔记本并逐 cell 精确复制代码。）
