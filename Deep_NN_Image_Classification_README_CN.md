# 深度神经网络用于图像分类：练习

完成本练习后，你将完成第 4 周的最后一个编程作业，也是本课程的最后一个编程作业！

你将使用在前一个作业中实现的函数来构建一个深度网络，并将其应用于“猫 / 非猫”二分类任务。期望能看到准确率的提升。

完成本作业后你将掌握：
- 构建并应用一个深度神经网络用于监督学习。

---

## 1 - 所需包

本练习中会用到的主要包：
- numpy：Python 科学计算的基础包；
- matplotlib：用于绘图；
- h5py：用于读取存储在 H5 文件中的数据集；
- PIL 和 scipy：用于在最后测试用你自己的图片；
- dnn_app_utils_v3：包含“逐步构建深度神经网络”作业中实现的辅助函数；
- 使用 `np.random.seed(1)` 保持随机操作可复现，有利于作业评测。

---

## 2 - 数据集

你将使用同样的“猫 vs 非猫”数据集（与“将逻辑回归作为神经网络”作业相同）。之前实现的模型在该数据集上的测试准确率为 70%。

问题描述：数据文件（data.h5）包含：
- 一个带标签（猫 = 1 / 非猫 = 0）的训练集，大小为 m_train；
- 一个带标签的测试集，大小为 m_test；
- 每张图像的形状为 (num_px, num_px, 3)，3 表示 RGB 三通道。

下面载入并熟悉数据。载入后可以查看示例图像（可更改索引查看不同图片）。

示例输出（载入数据并打印信息）：
- 训练样本数: 209
- 测试样本数: 50
- 每张图片大小: (64, 64, 3)
- train_x_orig shape: (209, 64, 64, 3)
- train_y shape: (1, 209)
- test_x_orig shape: (50, 64, 64, 3)
- test_y shape: (1, 50)

通常在将图像输入网络前，需要把图像展平并标准化（像素值归一化到 0 到 1）：
- 将训练和测试样本 reshape/flatten 成列向量，大小为 (num_px * num_px * 3, number of examples)
- 将像素值除以 255 进行标准化

注：12288 = 64 × 64 × 3，是展平后一个图像向量的长度。

---

## 3 - 模型架构

现在可以构建深度神经网络来区分猫与非猫。

将实现并比较两种模型：
- 一个 2 层神经网络
- 一个 L 层深度神经网络

接着比较它们在测试集上的表现，并尝试不同的 L 值。

### 3.1 - 2 层神经网络

模型��构示意图（文件中引用）：images/2layerNN_kiank.png

模型可概述为：INPUT -> LINEAR -> RELU -> LINEAR -> SIGMOID -> OUTPUT

详细结构说明：
- 输入是一张 (64,64,3) 的图片，被展平为一个 (12288,1) 的向量；
- 向量与权重矩阵 W^[1]（形状 (n^[1], 12288)）相乘，加偏置后经过 ReLU 得到第一层激活向量；
- 再次线性变换并加偏置，最后对输出做 sigmoid，若 > 0.5 则分类为猫。

### 3.2 - L 层深度神经网络

模型结构示意图（文件中引用）：images/LlayerNN_kiank.png

模型可概述为：[LINEAR -> RELU] × (L-1) -> LINEAR -> SIGMOID

详细结构说明：
- 输入展平为 (12288,1) 的向量；
- 依次对每一层做线性变换（W^[l] x + b^[l]），并对前 L-1 层使用 ReLU 激活；
- 最后一层使用 sigmoid 激活以得到二分类输出。

### 3.3 - 一般方法论

构建模型通常遵循下列步骤：
1. 初始化参数 / 定义超参数；
2. 在 num_iterations 循环中：
   a. 前向传播；
   b. 计算代价（cost）；
   c. 反向传播；
   d. 使用反向传播结果更新参数；
3. 使用训练得到的参数进行预测。

---

## 4 - 两层神经网络（练习说明）

在该部分，你需��使用之前作业中实现的辅助函数来构建一个结构为 LINEAR -> RELU -> LINEAR -> SIGMOID 的两层网络。模型训练过程中会打印并绘制代价的变化曲线，并可用于对训练集和测试集进行预测与评估。

示例常量：
- n_x = 12288 （输入单元数）
- n_h = 7
- n_y = 1

训练结束后，你可以看到训练集和测试集上的预测准确率。文中给出了一组预期输出供参考（例如训练集准确率可达 1.0，测试集约 0.72），并说明较少迭代（例如 1500 次）可能在测试集上取得更好准确率，这是“早停”（early stopping）的一个例子。

---

## 5 - L 层神经网络（练习说明）

在该部分，你需要实现一个通用的 L 层网络（[LINEAR -> RELU] × (L-1) -> LINEAR -> SIGMOID），并训练一个 4 层的示例网络（layers_dims = [12288, 20, 7, 5, 1]）。

训练过程中会显示代价随迭代的变化（并绘图）。预期训练输出示例（供参考）：
- 初始代价（iteration 0）：约 0.771749
- 经过多次迭代后代价下降（例如在 iteration 2400 时约 0.092878）

训练后在训练集上的准确率约为 98.56%，在测试集上的准确率约为 80%。通常 L 层网络比两层网络在该任务上表现更好。

---

## 6 - 结果分析

可以查看被 L 层模型错误分类的若干测试图片以便分析模型弱点。模型常见的失误类型包括：
- 猫的身体处于不寻常姿势；
- 猫与背景颜色相近，导致难以区分；
- 不常见的猫颜色或品种；
- 相机角度问题；
- 图片亮度问题；
- 尺度变化（猫在图像中非常大或非常小）。

---

## 7 - 使用你自己的图片进行测试（可选 / 非评分）

你可以用自己的图片测试训练好的模型，步骤：
1. 在 Jupyter 界面点击 “File” -> “Open”，进入 Coursera Hub；
2. 将你的图像放入该 Notebook 的目录下的 images 文件夹；
3. 修改代码中你的图像文件名；
4. 运行对应代码，检查模型输出（1 = 猫，0 = 非猫）。

若要测试，请将图像预处理为与训练同样的尺寸（num_px × num_px），并做相同的展平与标准化处理。

---

## 参考资料

- 关于在 IPython 中自动重新加载外部模块的说明（autoreload）：http://stackoverflow.com/questions/1907993/autoreload-of-modules-in-ipython
